# Task 0: Basic components

This task splits the main entry point `App.jsx` into modular functional components: `Header`, `Login`, and `Footer`, along with their individual CSS stylesheets and spec test files.
